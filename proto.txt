1. Create context.js and wrap our whole app inside index.js

2. Set up pages with react router inside App.js

3. Set up the easy pages inside the pages folder error,about etc. (No js backend for them)

4. Inside home.js return the searchform and cocktail list

5.  Set up basic state values inside Context.js and pass down do the app
ex: const [loading, setLoading] = useState(true);
    <AppContext.Provider value={{ loading, searchTerm, cocktails, setSearchTerm }}>

6.Inside the components we deconstructure the values needed using the global context hook
ex: const CocktailList = () => {
  const { cocktails, loading } = useGlobalContext();

7. Inside the context.js make the fetch function to get data from API everytime we put search inside the input
and call it whenever the search term changes(when we put new values in the input)

8. Inside cocktail.js deconsturct data from cocktailsList and display.
ex: const Cocktail = ({ image, name, id, info, category, glass }) => {
  return (
    <article className="cocktail"> etc...

9. Set up search form

10. set up single cocktail componenet

11. to do add more functionality

12. connect project  to git repo and netlify automaticaly updates - Continuous Deployment